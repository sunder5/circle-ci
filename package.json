{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./chain-rx-server/app.js"
  },
  "dependencies": {
    "express": "^4.16.3",
    "bcrypt": "^2.0.0",
    "sawtooth-sdk": "1.0.4"
  }
}
